<Window x:Class="RestaurantManagementSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hệ Thống Quản Lý Nhà Hàng" 
        Height="800" Width="1200" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">

    <Window.Resources>
        <Style x:Key="ToggleMenuButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#3D4446"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A4B0BE"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                            CornerRadius="12" Margin="10,2,10,2">
                            <Grid>
                                <Rectangle x:Name="indicator" HorizontalAlignment="Left" 
                                       Width="4" Height="20" Fill="#00B894" 
                                       RadiusX="2" RadiusY="2" Visibility="Collapsed"/>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D4446"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="border" Property="Background" Value="#3D4446"/>
                                <Setter Property="Foreground" Value="#00B894"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF5E57"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                            CornerRadius="12" Margin="10,2,10,2">
                            <Grid>
                                <Rectangle x:Name="indicator" HorizontalAlignment="Left" 
                                       Width="4" Height="20" Fill="#FF5E57" 
                                       RadiusX="2" RadiusY="2" Visibility="Collapsed"/>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4A3032"/>
                                <Setter Property="Foreground" Value="#FF7675"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="border" Property="Background" Value="#4A3032"/>
                                <Setter Property="Foreground" Value="#FF7675"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid CacheMode="BitmapCache">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="NavColumn" Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2D3436" CornerRadius="0,25,25,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Button x:Name="btnToggleMenu" Style="{StaticResource ToggleMenuButtonStyle}" 
                            Click="btnToggleMenu_Click" HorizontalAlignment="Right" Margin="0,12,15,0">
                        <TextBlock x:Name="txtToggleIcon" Text="◧" FontSize="15" Foreground="#A4B0BE" FontWeight="Bold"/>
                    </Button>
                </Grid>

                <StackPanel x:Name="UserInfoSection" Grid.Row="1" Margin="0,20,0,30">
                    <Grid HorizontalAlignment="Center">
                        <Border Width="85" Height="85" CornerRadius="50" Background="#3D4446" 
                                BorderBrush="#1E272E" BorderThickness="1">
                            <TextBlock x:Name="txtAvatarInitial" Text="H" Foreground="#00B894" 
                                       FontSize="32" FontWeight="Bold" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <Ellipse Width="15" Height="15" Fill="#2ECC71" Stroke="#2D3436" StrokeThickness="2"
                                 VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,5,5"/>
                    </Grid>
                    <TextBlock x:Name="txtGreeting" Text="Xin chào," Foreground="#636E72" FontSize="12" 
                               HorizontalAlignment="Center" Margin="0,15,0,2"/>
                    <TextBlock x:Name="txtUserDisplayName" Text="QUẢN TRỊ VIÊN" Foreground="White" 
                               FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Border Background="#1E272E" CornerRadius="12" Padding="12,4" 
                            HorizontalAlignment="Center" Margin="0,8,0,0">
                        <TextBlock x:Name="txtRoleBadge" Text="👑 Administrator" Foreground="#00B894" 
                                   FontSize="10" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden">
                    <StackPanel>
                        <TextBlock x:Name="lblMainMenu" Text="MAIN MENU" Foreground="#485460" FontWeight="Bold" 
                   Margin="25,10,0,15" FontSize="10"/>

                        <Button x:Name="btnHome" Style="{StaticResource NavButtonStyle}" Click="btnHome_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🏠" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" x:Name="txtHome" Text="Trang Chủ" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </Grid>
                        </Button>

                        <Button x:Name="btnPhucVu" Style="{StaticResource NavButtonStyle}" Click="btnPhucVu_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🍽️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" x:Name="txtPhucVu" Text="Phục Vụ Món" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </Grid>
                        </Button>

                        <Button x:Name="btnThuNgan" Style="{StaticResource NavButtonStyle}" Click="btnThuNgan_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="📠" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" x:Name="txtThuNgan" Text="Thu Ngân" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </Grid>
                        </Button>

                        <TextBlock x:Name="lblUserSettings" Text="USER SETTINGS" Foreground="#485460" FontWeight="Bold" 
                   Margin="25,25,0,15" FontSize="10"/>

                        <Button x:Name="btnDoiMatKhau" Style="{StaticResource NavButtonStyle}" Click="btnDoiMatKhau_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🔐" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" x:Name="txtDoiMatKhau" Text="Bảo Mật" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </Grid>
                        </Button>

                        <Button x:Name="btnDangXuat" Style="{StaticResource LogoutButtonStyle}" Click="btnDangXuat_Click">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="➜]" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" x:Name="txtDangXuat" Text="Đăng Xuất" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="3" Background="#1E272E" CornerRadius="25,25,0,0" Padding="15">
                    <StackPanel x:Name="spAdminSection">
                        <Button x:Name="btnAdmin" Click="btnAdmin_Click" Height="45" Background="#00B894">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                        <Grid>
                                            <TextBlock x:Name="txtAdminBtn" Text="SYSTEM CONTROL" HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center" FontSize="12" 
                                                       FontWeight="Bold" Foreground="White"/>
                                            <TextBlock x:Name="icoAdminBtn" Text="⚙️" Visibility="Collapsed" HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center" FontSize="18" Foreground="White"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Window}}" Value="Collapsed">
                                            <Setter TargetName="txtAdminBtn" Property="Visibility" Value="Collapsed"/>
                                            <Setter TargetName="icoAdminBtn" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="75"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" Margin="20,10,20,0" CornerRadius="15"
                    BorderBrush="#EDF0F2" BorderThickness="1">
                <Grid Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="txtCurrentIcon" Text="🏠" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="txtCurrentTab" Text="Dashboard" FontSize="18" FontWeight="Bold" Foreground="#2D3436"/>
                            <TextBlock x:Name="txtBreadcrumb" Text="Trang chủ / Bảng điều khiển" FontSize="11" Foreground="#A4B0BE"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Border Background="#F1F2F6" CornerRadius="10" Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📅" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock x:Name="txtDateDisplay" Text="" VerticalAlignment="Center" Foreground="#2F3542" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20">
                <Border Background="White" CornerRadius="20" BorderBrush="#EDF0F2" BorderThickness="1">
                    <Grid>
                        <StackPanel x:Name="WelcomePanel" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Border Width="280" Height="220" Margin="0,0,0,10">
                                <Image x:Name="imgChef" Stretch="Uniform" RenderOptions.BitmapScalingMode="LowQuality"/>
                            </Border>
                            <TextBlock Text="L'EXQUISITE CUISINE" FontSize="14" FontWeight="Bold" Foreground="#00B894" HorizontalAlignment="Center" Margin="0,0,0,5"/>

                            <TextBlock x:Name="txtWelcomeTitle" Text="Chào mừng bạn quay trở lại" FontSize="28" FontWeight="Light" Foreground="#2D3436" HorizontalAlignment="Center"/>

                            <Border Height="1" Width="60" Background="#DFE6E9" Margin="0,10,0,15"/>

                            <TextBlock x:Name="txtWelcomeQuote" Text="“Nấu ăn là nghệ thuật, quản lý là một khoa học.”" FontSize="15" FontStyle="Italic" Foreground="#B2BEC3" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <ContentControl x:Name="MainContent"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Grid x:Name="ConfirmDialog" Grid.ColumnSpan="2" Visibility="Collapsed" Panel.ZIndex="1000">
            <Border Background="#802D3436" MouseLeftButtonDown="btnCancelLogout_Click"/>
            <Border Background="White" CornerRadius="20" Width="380" Height="220" 
                    VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.2"/>
                </Border.Effect>
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="⚠️" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Xác nhận đăng xuất" FontSize="18" FontWeight="Bold" Foreground="#2D3436" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="Bạn có chắc chắn muốn thoát khỏi hệ thống quản lý nhà hàng? Các phiên làm việc hiện tại sẽ được đóng lại." 
                               Foreground="#636E72" FontSize="14" TextWrapping="Wrap" LineHeight="20"/>

                    <UniformGrid Grid.Row="2" Columns="2" Margin="0,10,0,0">
                        <Button Content="Hủy bỏ" Height="40" Margin="0,0,10,0" Cursor="Hand" Click="btnCancelLogout_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#F1F2F6" CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Content="Đăng xuất" Height="40" Foreground="White" Cursor="Hand" Click="btnConfirmLogout_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="#FF5E57" CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </UniformGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>